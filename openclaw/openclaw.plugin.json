{
  "id": "tunpilot",
  "name": "TunPilot",
  "description": "Agent-native proxy node management — Skill bundle for deploying and managing Hysteria2 / Xray nodes via the TunPilot REST API and CLI.",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "tunpilotUrl": { "type": "string" },
      "authToken": { "type": "string" }
    },
    "required": ["tunpilotUrl", "authToken"]
  },
  "uiHints": {
    "tunpilotUrl": { "label": "TunPilot URL", "placeholder": "https://tunpilot.example.com" },
    "authToken": { "label": "Auth Token", "sensitive": true }
  }
}
